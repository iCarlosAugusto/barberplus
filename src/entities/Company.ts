export interface Company {
    id: string;
    name: string;
    slug: string;
    description: string;
    address: string;
    phone: string;
    email: string;
    createdAt: string;
    updatedAt: string;
}
